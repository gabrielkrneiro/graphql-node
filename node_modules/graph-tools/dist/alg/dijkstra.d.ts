import { Graph, IEdgeObj } from "../graph";
export interface IEntry {
    [key: string]: number | string;
    distance: number;
}
export interface IDijkstraResult {
    [v: string]: IEntry;
}
export interface IDijkstraAllResult {
    [v: string]: IDijkstraResult;
}
export declare type IWeightFn = (edge: IEdgeObj) => number;
export declare type IEdgeFn = (v: string) => IEdgeObj[] | undefined;
declare function dijkstra(g: Graph, source: string, weightFn?: IWeightFn, edgeFn?: (v: string) => IEdgeObj[] | undefined): IDijkstraResult;
declare function dijkstraAll(g: Graph, weightFn: IWeightFn, edgeFn?: IEdgeFn): IDijkstraAllResult;
export { dijkstra, dijkstraAll };
