import { Graph } from "../graph";
export interface IVisited {
    [v: string]: {
        index: number;
        lowLink: number;
        onStack: boolean;
    };
}
declare function tarjan(g: Graph): string[][];
export { tarjan };
