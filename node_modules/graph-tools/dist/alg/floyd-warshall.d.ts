import { Graph } from "../graph";
import { IEdgeFn, IWeightFn } from "./dijkstra";
export interface IResult {
    [v: string]: {
        [v: string]: {
            distance: number;
            predecessor?: string;
        };
    };
}
declare function floydWarshall(g: Graph, weightFn?: IWeightFn, edgeFn?: IEdgeFn): IResult;
export { floydWarshall };
