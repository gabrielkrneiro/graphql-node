import { Graph } from "../graph";
export declare enum Order {
    pre = "pre",
    post = "post",
}
declare function dfs(g: Graph, vs: string[] | string, order: Order): string[];
export { dfs };
